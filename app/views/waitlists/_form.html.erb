<%= form_with(model: waitlist, local: true) do |form| %>
  <% if waitlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(waitlist.errors.count, "error") %> prohibited this waitlist from being saved:</h2>

      <ul>
      <% waitlist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :requested_seats %>
    <%= form.number_field :requested_seats %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :tour_id %>
    <%= form.text_field :tour_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
