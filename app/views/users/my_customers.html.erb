<p id="notice"><%= notice %></p>

<h1>Customers Booked My Tours</h1>
<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th>Role</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @users.each do |user| %>
    <tr>
      <td><%= user.name %></td>
      <td><%= user.email %></td>
      <td><%= user.role %></td>
      <td><%= link_to 'Show', user %></td>
    </tr>
  <% end %>
</table>

<br/>
<br/>

<%= link_to 'Home', home_index_url %> |
<%= link_to 'Back', tours_url %>
